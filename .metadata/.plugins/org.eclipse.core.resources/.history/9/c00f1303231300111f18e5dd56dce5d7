package apicontractdesign.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestHeader;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import apicontractdesign.service.UserService;

/**
 * Controller 계층
 * - HTTP 요청/응답 담당
 * - 외부 Open API 호출은 Service로 위임
 */
@RestController
@RequestMapping("/users")
public class UserController {

    private final UserService userService;

    public UserController(UserService userService) {
        this.userService = userService;
    }

    /**
     * 네이버 회원정보 조회 API
     * Authorization 헤더로 access token 전달
     */
    @GetMapping("/naver/profile")
    public String getNaverUserProfile(
            @RequestHeader("Authorization") String authorization
    ) {
        return userService.getNaverUserProfile(authorization);
    }
}